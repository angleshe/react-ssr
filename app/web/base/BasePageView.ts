import { Component } from 'react';
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export class BasePageView<P = {}, S = {}, SS = any> extends Component<P, S, SS> {
  // static getInitialProps (params: any) {}
}
