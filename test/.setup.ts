// import { app } from 'egg-mock/bootstrap';
// import factorise from './factories';
// import { Context } from 'egg';
// const ctx: Context = app.mockContext();
// before(() => factorise(ctx));
// afterEach(async () => {
//   await Promise.all([
//     ctx.model.User.destroy({ truncate: true, force: true })
//   ])
// })
